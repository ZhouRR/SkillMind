{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.skillmind.local/runs/cancel/v1/response.schema.json",
  "title": "Cancel Run response v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "run_id",
    "project_id",
    "status",
    "row_version",
    "cancellation"
  ],
  "properties": {
    "run_id": { "type": "string", "format": "uuid" },
    "project_id": { "type": "string", "format": "uuid" },
    "status": {
      "enum": [
        "QUEUED",
        "PREPARING",
        "RUNNING",
        "RETRY_PENDING",
        "WAITING_PERMISSION",
        "WAITING_FOR_INPUT",
        "WAITING_FOR_APPROVAL",
        "SUCCEEDED",
        "FAILED",
        "CANCELLED"
      ]
    },
    "row_version": { "type": "integer", "minimum": 1 },
    "cancellation": { "enum": ["REQUESTED", "CANCELLED"] }
  }
}
