name: ${COMPOSE_PROJECT_NAME:-skillmind}

x-backend-runtime: &backend-runtime
  image: skillmind/backend:0.1.0
  # 古い Compose run でも CLI flag に依存せず local image のみ使う。
  pull_policy: never
  env_file:
    - ${ENV_FILE:-.env}
  environment:
    SKILLMIND_CONTEXT_PATH: ${SKILLMIND_CONTEXT_PATH:-/skillmind}
    TZ: ${SKILLMIND_TIMEZONE:-Asia/Tokyo}
  restart: unless-stopped
  init: true
  security_opt:
    - no-new-privileges:true

services:
  web:
    build:
      context: .
      dockerfile: web/Dockerfile
      args:
        SKILLMIND_CONTEXT_PATH: ${SKILLMIND_CONTEXT_PATH:-/skillmind}
    image: skillmind/web:0.1.0
    environment:
      TZ: ${SKILLMIND_TIMEZONE:-Asia/Tokyo}
    restart: unless-stopped
    init: true
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
      - /var/cache/nginx:size=32m,uid=101,gid=101
    security_opt:
      - no-new-privileges:true
    networks:
      - edge
    labels:
      traefik.enable: "true"
      traefik.docker.network: ${TRAEFIK_NETWORK:-traefik-public}
      traefik.http.routers.skillmind-web.rule: Host(`${SKILLMIND_HOST}`) && (Path(`${SKILLMIND_CONTEXT_PATH:-/skillmind}`) || PathPrefix(`${SKILLMIND_CONTEXT_PATH:-/skillmind}/`))
      traefik.http.routers.skillmind-web.entrypoints: ${TRAEFIK_ENTRYPOINT:-websecure}
      traefik.http.routers.skillmind-web.tls: "${TRAEFIK_TLS:-true}"
      traefik.http.routers.skillmind-web.priority: "10"
      traefik.http.routers.skillmind-web.middlewares: skillmind-web-context@docker
      traefik.http.middlewares.skillmind-web-context.stripprefix.prefixes: ${SKILLMIND_CONTEXT_PATH:-/skillmind}
      traefik.http.services.skillmind-web.loadbalancer.server.port: "8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  api:
    <<: *backend-runtime
    build:
      context: .
      dockerfile: backend/Dockerfile
    command:
      - python
      - -m
      - uvicorn
      - skillmind.api.main:app
      - --host=0.0.0.0
      - --port=8000
      - --proxy-headers
      - --forwarded-allow-ips=${SKILLMIND_TRUSTED_PROXY_CIDRS:-172.16.0.0/12}
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      object-storage-init:
        condition: service_completed_successfully
    networks:
      - edge
      - backend
    labels:
      traefik.enable: "true"
      traefik.docker.network: ${TRAEFIK_NETWORK:-traefik-public}
      traefik.http.routers.skillmind-api.rule: Host(`${SKILLMIND_HOST}`) && (Path(`${SKILLMIND_CONTEXT_PATH:-/skillmind}/api`) || PathPrefix(`${SKILLMIND_CONTEXT_PATH:-/skillmind}/api/`))
      traefik.http.routers.skillmind-api.entrypoints: ${TRAEFIK_ENTRYPOINT:-websecure}
      traefik.http.routers.skillmind-api.tls: "${TRAEFIK_TLS:-true}"
      traefik.http.routers.skillmind-api.priority: "100"
      traefik.http.routers.skillmind-api.middlewares: skillmind-api-context@docker
      traefik.http.middlewares.skillmind-api-context.stripprefix.prefixes: ${SKILLMIND_CONTEXT_PATH:-/skillmind}
      traefik.http.services.skillmind-api.loadbalancer.server.port: "8000"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          import urllib.request;
          urllib.request.urlopen('http://127.0.0.1:8000/internal/health/live', timeout=3)
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  worker:
    <<: *backend-runtime
    command:
      - python
      - -m
      - arq
      - skillmind.worker.settings.WorkerSettings
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      object-storage-init:
        condition: service_completed_successfully
    networks:
      - backend
      - egress
    volumes:
      - run-data:/var/lib/skillmind/runs

  migrate:
    <<: *backend-runtime
    command: ["alembic", "upgrade", "head"]
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - backend

  postgres:
    image: postgres:17.9-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-skillmind}
      POSTGRES_USER: ${POSTGRES_USER:-skillmind}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}
      TZ: ${SKILLMIND_TIMEZONE:-Asia/Tokyo}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    security_opt:
      - no-new-privileges:true

  redis:
    image: redis:8.2-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes", "--save", "60", "1"]
    environment:
      TZ: ${SKILLMIND_TIMEZONE:-Asia/Tokyo}
    volumes:
      - redis-data:/data
    networks:
      - backend
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    security_opt:
      - no-new-privileges:true

  object-storage:
    image: quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z
    restart: unless-stopped
    command: ["server", "/data", "--console-address", ":9001"]
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?set MINIO_ROOT_USER in .env}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?set MINIO_ROOT_PASSWORD in .env}
      TZ: ${SKILLMIND_TIMEZONE:-Asia/Tokyo}
    volumes:
      - object-storage-data:/data
    networks:
      - backend
    security_opt:
      - no-new-privileges:true

  # 空の volume を初回起動した場合でも、API/Worker の upload 前に bucket を用意する。
  # `mc mb --ignore-existing` は既存 bucket を変更しないため、再作成時にも安全に実行できる。
  object-storage-init:
    image: quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z
    restart: "no"
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - >-
        attempts=0;
        until mc alias set skillmind http://object-storage:9000
        "$${MINIO_ROOT_USER}" "$${MINIO_ROOT_PASSWORD}" >/dev/null 2>&1; do
        attempts=$$((attempts + 1));
        if [ "$$attempts" -ge 60 ]; then
        echo 'MinIO initialization failed: check service readiness and credentials.' >&2; exit 1;
        fi; sleep 2; done;
        mc mb --ignore-existing "skillmind/$${SKILLMIND_OBJECT_STORAGE_BUCKET}"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?set MINIO_ROOT_USER in .env}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?set MINIO_ROOT_PASSWORD in .env}
      SKILLMIND_OBJECT_STORAGE_BUCKET: ${SKILLMIND_OBJECT_STORAGE_BUCKET:?set SKILLMIND_OBJECT_STORAGE_BUCKET in .env}
      TZ: ${SKILLMIND_TIMEZONE:-Asia/Tokyo}
    depends_on:
      object-storage:
        condition: service_started
    networks:
      - backend
    security_opt:
      - no-new-privileges:true

networks:
  edge:
    external: true
    name: ${TRAEFIK_NETWORK:-traefik-public}
  backend:
    internal: true
  egress: {}

volumes:
  postgres-data: {}
  redis-data: {}
  object-storage-data: {}
  run-data: {}
