---
name: execution-plan-generator
description: RV を通過した保存済み Markdown から業務テストの手順と期待結果を固定し、実行時の画面情報を使って MCP を呼び出し、実測結果と証跡を保存する。テスト仕様書に基づく自動テストの計画・実行・中断後の継続に使用する。
---

# テスト計画・実行

## タスクと入力

公開タスクは「テスト計画・実行」の一つとし、計画作成、MCP による実行、成果物の保存、中断後の継続まで担当する。既存のプラットフォーム登録との互換性のため Skill の識別子は維持する。内部工程ごとの起動や画面情報ファイルの事前提出を利用者に求めない。計画のみという明示依頼では保存までとし、アプリを操作しない。他の Skill は参照・呼び出しせず、最終的なテスト判定の起動はプラットフォームに任せる。

対象は「文書範囲」で選択した RV 済み Markdown、または実行コンテキストの文書参照から特定する。選択済みならタスク入力は `{}` とし、パス・ID・本文を再入力させない。`test_automation.test_run`、`test_document`、`test_artifact` の保存記録から、選択した Markdown に対応する `testRunId`、`documentId`、`specVersion` を引き継ぐ。一意でなければ対象を確認し、過去の PASS を暗黙に選ばない。

仕様本文の入力はこの Markdown のみとする。元 Excel の取得・読取・再変換・版照合を行わず、利用者に Excel の提出やパス・シート・セル情報の入力を求めない。元 Excel のファイル・保存先・Version ID・ETag・シート名・セル座標が提供されなくても、それを理由に停止しない。

接続済みの MinIO、PostgreSQL、Schema 検証ツール、実行対象の Runner／MCP を使う。DB の表はすべて `test_automation` Schema に属する。登録 appId と環境・対象ビルドの版は、接続済み環境の公開設定・読取結果、または監査済みの回答から取得する。接続参照はプラットフォームの binding を使い、仕様書への追記や秘密情報の入力を求めない。未提供の環境設定を工具清單・アプリ名から推測しない。

## 新規テストと旧実行の復旧

新しく開始された Run は、利用者が旧実行の継続・記録復旧を明示した場合を除き、選択 Markdown に基づく独立したテストとして扱う。旧計画や `ABORTED` の履歴が見つかっただけで復旧へ切り替えない。待機への回答で「現環境版で再テスト」が選ばれた場合も、現在の Run で新規計画・実行の準備を続ける。「別依頼」は旧実行とは別の業務実行を意味し、同じタスクを起動し直すよう求めない。SKM の Run ID と RV 由来の `testRunId` は別の識別子であり、新しい Run を作っても既存計画の DB キーは変わらない。

旧処理の停止と占有競合の解消が確認され、新規テストの現在の前提条件が成立するなら、過去の `UNKNOWN` や未保存成果だけを理由に新規テストを止めない。原操作の結果は不明のまま保存し、旧要求を再送・成功扱いせず、新しい `executionId` と操作 ID・時間枠を使う。旧処理の稼働、未解消の pending、現在の対象状態への具体的な影響が残る場合は、その競合・前提だけを確認する。中断状態への DB 更新だけで原操作の終了や結果確定を推測しない。

旧計画本文・操作記録の追加読取は、その計画の再利用または明示された復旧に必要な場合だけ求める。Markdown から独立した新規計画を作るために、旧記録の復元を必須にしない。復旧を打ち切る指示では不足と結果不明を報告して終了し、新規テストや旧データの書換えへ勝手に移らない。

## 前処理

1. 選択した Markdown に対応する保存記録で RV が `COMPLETED` ＋ `PASS` であることを確認し、RV 済みの保存済み `MARKDOWN` を取得する。判定と文書の対応は登録記録で確認し、元ファイルへのアクセスを確認条件にしない。
2. `test_execution_plan` の同じ `(test_run_id, document_id)` と実際の表構造・一意制約を確認する。既存計画は履歴として参照し、現在の版を取得後に再利用可否を判断する。入力・版・ハッシュが一致し、許可された範囲で本文を取得・検証できる完了済み計画だけを再利用する。他の処理が所有する `RUNNING` を重ねず、過去の予約を現在の利用権としない。旧候補生成や Git 公開を前提にしない。
3. 実際に提供された MCP ツールと入力契約、対象アプリ、証跡取得・文書庫保存機能を確認する。Resources の読取権限だけではツールを実行できない。ツール呼出し機能がない場合は不足として返し、架空の URI やツール名、未許可の接続手段で代替しない。
4. `mcp.tools/v1` が提供される場合は `reserve_desktop: true` で工具契約・接続参照・サービス版と本 Run のデスクトップ予約を取得する。`desktop.lease_ref` を `environmentLease` に用い、`pending_operation` が true なら原操作の照合まで進めない。対象 Windows デスクトップは実行中の専用利用が手配済みで、手動操作・別システムの操作は入らないことを既定の運用前提とし、開始時・継続時に利用者への確認を求めない。SKM の予約は同一 endpoint の Run 間だけの排他であり、外部排他の技術的な保証とは区別する。`external_exclusivity: NOT_VERIFIED` だけを理由に停止・質問しない。実際の占有競合や外部操作を検知した場合、または必要な予約を取得できない場合は、後続操作を停止して具体的な不足・競合を報告する。
5. 許可済み工具に `get_environment` があれば、その入力 Schema に従って `mcp.query/v1` から呼び出し、`applications` の登録 appId・sutBuild、`environmentVersion`、`runnerVersion`、時間制限と同時利用条件を取得する。別名の環境取得工具がある場合も実際の説明・契約に従う。取得できる項目を利用者に再入力させない。版の取得にアプリの起動・画面検査を前提としない。`server.version` は MCP サービス版として `runnerVersions["mcpServer:" + server.name]` に固定し、実操作が返す `runnerVersion` と区別する。モデルは Brief の `runtime_metadata.model` を `modelVersion` とする。これは設定モデル ID であり内部ビルド番号ではない。環境版・対象ビルドなど取得経路がない必須値は、不足する項目だけをまとめて確認する。未提供の機能名で呼び出したり、`inspect_window` を環境設定の取得手段にしたりしない。

## 計画の固定

版が異なる新規テストでは旧計画と旧実行の固定値を維持し、現在の版で別計画を登録する。ただし、実際の DB と添付 Schema がその保存・実行からの参照を表現できることを先に確認する。`test_execution_plan` が `(test_run_id, document_id)` の主キーで一件しか保持できず、同じキーの旧計画がある場合は、複数の固定計画を保持するための DB／Schema 対応不足として `BLOCKED` を返す。現 Run で不足を明示して終了し、同じ入力での再起動や旧結果の照合では解消しないことを説明する。未定義の `plan_id`・列を使わず、旧計画の上書き・削除、RV ID の付替え、ダミーの RV 記録で回避しない。

1. 計画を `RUNNING` とし、開始日時・仕様版を記録する。生成前に `attempt_count` を増やし、構造化出力の修正は初回を含め最大 3 回とする。この回数を UI 操作の再実行回数に流用しない。
2. Markdown のケース、操作順、前提条件、データ、期待結果を保持し、業務ケースを追加しない。ID がなければ文書内の位置から安定した ID を付ける。`sourceRefs` は Markdown 内の見出し・ケース ID・本文の引用で示す。見出しがなければ文書内のケース・手順の位置を使い、存在しない要求 ID・期待値を創作しない。
3. [execution-plan.schema.json](schemas/execution-plan.schema.json)（`schemaVersion: 1.0`）で全ケース・業務ステップを固定する。`action` と `expectedResult` は原文に基づく。`runner`、`operation`、MCP の `connectionRef` は確認済みの実行手段を使う。`inputs` には既知の入力と画面上の対象を特定する説明を保持し、実行時に解決する内容を区別する。これは Agent が読む計画であり、未解決の説明を MCP 引数へそのまま送らない。必要な観測・補助操作は元の `stepId` に関連付ける。
4. 依存順序、確認済み予約参照、並列度 1、時間制限、Evidence、再試行・クリーンアップ条件を定める。ケース時間は環境・タスクの指定を優先し、指定がなければ 1800 秒、単独操作は 15 秒を既定とし、実際の工具上限内に収める。ケース時間は最初のアプリ準備からの経過時間として記録し、承認・中断待ちも含め、期限後は新しい操作を提案しない。継続時に時計をリセットしない。Agent の attempt 上限、MCP 通信上限、Runner の単独操作上限をケース全体の強制停止保証とみなさず、進行中の操作は原 ID で状態照合する。送信・登録等の非冪等操作は自動再試行しない。仕様にないデータ削除・終了操作を追加しない。
5. `sourceMarkdown` に Markdown の文書参照と取得バイト列の SHA-256 を保存する。`specVersion` は対象文書の RV 登録記録からそのまま引き継ぐ追跡値とし、元ファイルを取得して再計算・検証しない。Markdown のハッシュは `sourceMarkdown.sha256` に保持し、既存の `specVersion` を置き換えない。`versionSnapshot` は Skill の公開版または checksum を `skillVersion` とし、環境・対象ビルド・Runner・モデルの版とともに固定する。ハッシュは `sha256:` ＋小文字 16 進数 64 桁とする。
6. Schema・format と、原文の網羅、期待結果の保持、ID・依存関係、利用可能な操作、実行時に対象を解決できる条件を検証する。ブロッカーがなく全検証に成功した場合だけ `validation_result.valid = true` とする。

既定の `実行計画/{testRunId}/{documentId}/実行計画.json` に保存し、実際の `documentLibraryId`、`bucket`、`objectKey`、`sha256` を `test_execution_plan.plan_ref` に記録する。件数・固定版・検証結果・阻害理由も DB に保存し、保存・回読の成功後に計画を `COMPLETED` として状態と `finished_at` を同時更新する。実行中は計画の本文・期待結果・ハッシュを変更しない。候補 IR、手順配列の複製、独立した計画生成結果は作らない。

既存 DB が旧候補生成への外部キー、Git の版、未生成の `steps_ref` / `result_ref` を要求する場合は、移行が必要として実行前に停止する。ダミー記録で制約を満たさない。計画の完了はテスト実行の完了と区別する。

## 画面確認と MCP 実行

1. 新しいテスト実行ごとに `executionId`（UUID）を発行し、計画に対応する `test_execution` へ固定版と `QUEUED` を登録する。開始時に `RUNNING` と `started_at` を記録する。同一実行の継続では既存 ID を使い、`testRunId`・`documentId` は作り直さない。登録・計画保存が未完了ならアプリを操作しない。
2. FlaUI では実行登録後に `open_application` を提案して登録アプリを準備する。起動には `requestId: "${effect_id}"` を渡し、同じ ID の `get_step_status` で原要求の回执を確認する。`READY` の場合だけ `inspect_window` で現在画面と対象コントロールの操作可否を確認して業務ステップへ進む。保存済みの起動回执は現在の画面・生存を保証しない。取得済みの画面情報は画面変化や不足がない間は再利用する。同じ実行の既存準備回执が確認済みなら起動を繰り返さない。`inspect_window` はアプリを起動しない。ウィンドウ・コントロールを現在の情報から特定し、画面遷移後は再取得する。対象が複数なら観測で絞り込み、業務上の対象を一意に判断できなければ停止して確認する。
3. 元の業務ステップに必要な操作を `execute_step` で一つずつ提案し、承認済み操作をプラットフォーム経由で実行する。提案前に入力をツール契約と照合し、`executionId`・ケース ID・`stepId`・操作内容を記録する。提案にもこの対応を示し、送信前の要求と提案参照はプラットフォームの監査記録で保持する。`requestId` はプラットフォームが操作ごとの Effect UUID として供給するため、独自に生成せず、実値の代わりに呼出し引数の `requestId` へ予約値 `${effect_id}` を指定する。汎用 MCP 提案は `operation: "call"`、`target.locator` を実際の工具名とし、`/call` の値に `arguments` と `read_back` を指定する。`execute_step` の回読は `get_step_status` とし、同じ予約値を照会引数と `/requestId` の equals 条件に使い、`/status` は `COMPLETED / ERROR / TIMEOUT / CANCELLED / ABORTED` の one_of 条件で終端を確認する。回読の JSON Pointer と比較値は実際の `output_schema`、公開された返却契約または同じ工具の観測済み応答に基づく。入力引数の存在から返却フィールドを推測しない。`open_application` の回読も `get_step_status` を使い、同じ `/requestId`、登録済み `/appId`、`/status` の `READY / ERROR / TIMEOUT / CANCELLED / ABORTED` を確認する。失敗終端の一致は結果確定であり起動成功ではない。`READY` 以外では業務操作へ進まず異常時の記録へ移る。起動と各業務操作にはそれぞれ別の Effect が供給する ID を使う。正式な操作 ID を実行結果・監査記録から取得後、元の提案と業務ステップへ対応付けて操作記録を補完する。提案時点で未発行の `requestId` を要求して停止しない。
4. 応答が `RUNNING`、通信切断、応答不明なら、原要求に実際に渡されたことを確認できる `requestId` を許可された `get_step_status` で照会する。旧版の起動など要求 ID を送っていない操作では、Effect UUID を Runner の要求 ID とみなして照会しない。ID がまだ取得できない場合はプラットフォームによる元の操作の結果照合を使い、推測した ID や新しい提案で送り直さない。取消しの `cancel_step` にも取消対象の原 ID を渡し、提案の `/call` に `cancel_target` としてその Effect UUID を指定する。回読は同じ原 ID と終端・取消確認を条件とし、終了状態と現在画面を確認する。照会不能や操作済みか不明な状態では後続操作を止める。
5. 操作完了後に画面・値を再取得し、原文の期待条件に対応する実測を収集する。必要なスクリーンショットや読取結果も元の業務ステップへ結び付ける。表示内容と UI API の値表現が異なる場合は生値と解釈理由を残し、期待値の変更や一律の文字除去で一致させない。
6. 操作失敗・期待条件との不一致・前提未成立なら、その状態に依存する後続操作を止め、残りを `NOT_RUN` とする。正常な前提で実行できる独立ケースのみ継続できる。確認のために送信・登録を繰り返さず、時間制限で観測を打ち切る。

## 実行成果の保存と返却

- 既定の保存先は `実行結果/{executionId}/` とする。プロジェクト設定を優先し、継続時は同じ保存先を使う。`操作記録.json` に操作前の業務ステップ・要求内容と、取得後の提案参照・正式な操作 ID・応答・画面情報・時刻を保存する。プラットフォームの原監査記録と照合し、補助操作も元のケース・ステップへ対応付ける。未取得の ID を捏造せず、保存失敗時は次の操作へ進まない。
- FlaUI の単独操作結果を [execution-result.schema.json](schemas/execution-result.schema.json)（`schemaVersion: 1.0`）に集約して `実行結果.json` を作る。固定計画の全ケース・ステップを含め、未実行や実測不明を省略・成功扱いしない。`sourcePlan` は検証済み `plan_ref`、ID・`versionSnapshot` は登録値を引き継ぐ。
- Runner の `downloadPath` はローカル証跡の取得先であり文書庫参照ではない。許可された取得機能で実体を取得し、文書庫へ保存してから Evidence の参照を設定する。操作記録は `TRACE`、実測は `ACTUAL_OUTPUT` 等の契約上の種別で元ステップに関連付ける。取得できない証跡は不足として記録する。
- 全必須ステップを実行できた場合の `COMPLETED` は実行状態であり、テストの PASS ではない。操作異常や前提未成立による未完了は `ERROR`、実行の時間制限超過は `TIMEOUT`、取消し・中断は `ABORTED` にまとめる。FlaUI の `CANCELLED` は共通契約の `ABORTED` に対応付け、元の状態と理由を操作記録へ残す。
- 集約結果の Schema・format、計画との ID・版・ステップ対応、実測と保存済み Evidence を検証する。同じ種類の環境・対象ビルド・Runner 版が固定値と異なる場合は停止し、登録値で覆い隠さない。MCP サービス版と操作応答の `runnerVersion` は別の識別体系として扱い、文字列の違いだけで不整合にしない。固定時に未取得だった操作 Runner 版は操作記録に保存し、固定済み `versionSnapshot` を書き換えない。結果保存後、実際の文書参照を `test_execution.result_ref` に記録し、終端状態と `finished_at` を同時更新する。保存失敗を実行成功として返さず、取得済み結果から保存を再開できるようにする。
- ID、計画・実行の状態、件数、保存済みパス、未実行・証跡不足・異常理由を返す。MCP 呼出し成功や画面の変化だけから、仕様の確認範囲を超えた結果を断定しない。最終的な Verdict・原因分類は生成しない。

## 異常と継続

MCP の失敗は呼出し・回読・条件照合のどこで起きたかを区別し、元の Effect ID、診断 ID、取得済み観測の参照を保持する。起動済みでも回読失敗なら後続の入力・送信へ進まない。アプリ未起動・パス不一致・Windows セッション・読取権限は診断前に断定せず、応答不明の操作を新規 ID で再送しない。

プラットフォームが未送信または原操作の終端を確認して同じ Run を続行した場合、失敗が解消できなければ未実行ステップを `NOT_RUN` とし、実行を `ERROR`（時間超過は `TIMEOUT`、取消しは `ABORTED`）として `finished_at`・エラー・取得済み結果を既存 ID に保存・回読する。完了済み計画は維持する。結果未知は確定した操作失敗と区別する。プラットフォームが Run を終止した場合は後続の保存が実行されたとみなさず、原操作の核対と残った記録更新が必要であることを明示する。

計画段階の情報・機能不足は `BLOCKED`、ツール異常・修正上限は `FAILED` とする。実行登録後は実行表の状態を使い、存在しない `BLOCKED` を書き込まない。DB 書込みは利用環境の許可・承認手順と回読に従い、既に認められた範囲で工程ごとの追加確認を要求しない。DB が利用不能なら操作を止め、ID と保存済みパスを実行記録へ残す。

明示された旧実行の継続・記録復旧では、前の処理の終了、DB、操作記録、プラットフォームの原監査記録と照会可能な Runner の要求状態を照合する。既存 ID・固定版・期限を維持し、完了済みの RV・計画・操作を再実行せず未完了部分だけを回復する。記録保存だけが未完了なら取得済み結果から補完し、操作を再送しない。照会可能な原 ID や必要な記録がなければ復旧不能な範囲を明示する。未完了の計画生成を再開する場合だけ累積試行回数を保持して `finished_at` を NULL に戻し、完了済み計画は再開対象にしない。
